"""Command-line interface helpers for SABER.

The CLI is an operator-facing layer over storage, orchestration, reporting, and
approval workflows.

CLI modules should not run tools directly, parse raw tool output, or make agent
decisions.
"""

__all__: list[str] = []
